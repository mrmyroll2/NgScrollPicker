import { ItemStylePipe } from './item-style.pipe';

describe('ItemStylePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemStylePipe();
    expect(pipe).toBeTruthy();
  });
});
