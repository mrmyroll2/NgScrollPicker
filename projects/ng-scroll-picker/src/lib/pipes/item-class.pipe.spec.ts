import { ItemClassPipe } from './item-class.pipe';

describe('ItemClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemClassPipe();
    expect(pipe).toBeTruthy();
  });
});
