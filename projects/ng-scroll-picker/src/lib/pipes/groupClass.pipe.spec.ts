import { GroupClassPipe } from './groupClass.pipe';

describe('GroupclassPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupClassPipe();
    expect(pipe).toBeTruthy();
  });
});
